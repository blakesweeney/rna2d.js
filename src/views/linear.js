import View from '../view.js';

export default class Linear extends View {
  constructor(plot) {
    const defaults = new Map([
    ])
    super(plot, 'linear', defaults);
  }

  xCoord() {

  }

  yCoord() {

  }
}
